[package]
name = "nuclear-sim"
version = "1.0.0"
edition = "2021"
description = "Core nuclear simulation logic (isotopes, reactions, energy)"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
rand = "0.8"
