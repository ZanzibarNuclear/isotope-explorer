[package]
name = "nuclear-sim"
version = "0.2.0"
edition = "2021"
description = "Core nuclear simulation logic (isotopes, reactions, energy)"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
